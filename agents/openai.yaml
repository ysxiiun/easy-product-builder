interface:
  display_name: "Easy Product Builder"
  short_description: "通过连续追问，将一句话需求整理成 Product Spec 与 UI Spec"
  default_prompt: "使用这个技能通过多轮高价值追问，挖掘用户真实想法，生成或更新 Product-Spec.md；如果产品包含界面，再生成或更新 UI-Spec.md。不要默认产出额外文档，除非用户明确要求。"
